'use client'; import {useEffect,useState} from 'react'; type Ticket={ticket_public_id:string;subject:string;status:string;priority:string}; export default function Support(){const [rows,setRows]=useState<Ticket[]>([]);const [error,setError]=useState('');useEffect(()=>{fetch('/api/backend/support/tickets',{cache:'no-store'}).then(async r=>{if(!r.ok)throw new Error(r.status===401?'Sign in from Overview first.':'Support is unavailable.');setRows(await r.json())}).catch(e=>setError(e.message))},[]);return <><h1>Customer Support</h1>{error?<p className="error" role="alert">{error}</p>:<table><thead><tr><th>Ticket</th><th>Subject</th><th>Status</th><th>Priority</th></tr></thead><tbody>{rows.map(x=><tr key={x.ticket_public_id}><td>{x.ticket_public_id}</td><td>{x.subject}</td><td>{x.status}</td><td>{x.priority}</td></tr>)}</tbody></table>}</>}
