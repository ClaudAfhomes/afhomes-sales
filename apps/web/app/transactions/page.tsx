'use client'; import {useState} from 'react'; import {API} from '../../lib/api'; export default function Transactions(){const [result,setResult]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={idempotency_key:crypto.randomUUID(),branch_id:'BR-PH-0001',membership_id:f.get('membership'),type:'SALE',currency_code:'PHP',points_redeemed:0,items:[{product_code:'ROOM',description:'AFhomes service',quantity:1,unit_amount_minor:Number(f.get('amount'))}]};const r=await fetch(`${API}/api/v1/transactions`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setResult(JSON.stringify(await r.json(),null,2))}return <><h1>Point of Sale</h1><div className="card"><form onSubmit={submit}><label>Membership ID<input name="membership" required/></label><label>Amount (minor units)<input name="amount" type="number" min="0" defaultValue="125050" required/></label><button>Commit transaction</button></form>{result&&<pre>{result}</pre>}</div></>}
