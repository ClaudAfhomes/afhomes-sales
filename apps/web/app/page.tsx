import {api,API} from '../lib/api'; import DashboardClient from './dashboard-client';
export default async function Home(){const health=await api<Record<string,string>>('/health');return <><section className="hero"><span className="status">Secured operations</span><h1>Membership operations, in one place.</h1><p>Manage VIP cards, customers, points, transactions, support and audit-ready workflows.</p></section><DashboardClient/><section className="grid"><article className="card"><h2>System health</h2><p>API: <b>{health?.api||'offline'}</b></p><p>Database: <b>{health?.database||'unavailable'}</b></p><p>Realtime: <b>{health?.realtime||'unavailable'}</b></p><p className="muted">Endpoint: {API}</p></article></section></>}
