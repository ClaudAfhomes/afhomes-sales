import Image from 'next/image'; import {api,API} from '../lib/api'; import DashboardClient from './dashboard-client';
export default async function Home(){const health=await api<Record<string,string>>('/health');return <><section className="hero"><Image src="/afhomes/hotel/smart-wellness-hotel.webp" alt="AFhomes Smart Wellness Hotel" fill sizes="(max-width: 900px) 100vw, 80vw" priority/><div className="hero-overlay"/><div className="hero-content"><span className="eyebrow light">AFHOMES OPERATIONS</span><h1>Hospitality, membership,<br/>and service in harmony.</h1><p>One trusted workspace for VIP cards, customer care, points, transactions, and audit-ready operations.</p></div></section><DashboardClient/><section className="grid health-grid"><article className="card"><span className="eyebrow">PLATFORM HEALTH</span><h2>Connected and ready</h2><div className="health-list"><p><i className={health?.api==='healthy'?'ok':''}/>API <b>{health?.api||'offline'}</b></p><p><i className={health?.database==='connected'?'ok':''}/>Database <b>{health?.database||'unavailable'}</b></p><p><i className={health?.realtime==='ready'?'ok':''}/>Realtime <b>{health?.realtime||'unavailable'}</b></p></div><p className="muted endpoint">{API}</p></article><article className="card image-card"><Image src="/afhomes/restaurant/alm-resto.webp" alt="AFhomes ALM restaurant" fill sizes="500px"/><div><span className="eyebrow light">AFHOMES HOSPITALITY</span><h2>Every interaction, thoughtfully served.</h2></div></article></section></>}
