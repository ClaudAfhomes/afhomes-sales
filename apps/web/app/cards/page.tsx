'use client'; import {useEffect,useState} from 'react'; type VipCard={card_public_id:string;member_code:string;status:string;membership_id?:string}; export default function Cards(){const [cards,setCards]=useState<VipCard[]>([]);const [error,setError]=useState('');useEffect(()=>{fetch('/api/backend/cards',{cache:'no-store'}).then(async r=>{if(!r.ok)throw new Error(r.status===401?'Sign in from Overview first.':'Cards are unavailable.');setCards(await r.json())}).catch(e=>setError(e.message))},[]);return <><h1>VIP Cards</h1><p className="muted">Printed QR, NFC and member code identify one physical card.</p>{error?<p className="error" role="alert">{error}</p>:<table><thead><tr><th>Card</th><th>Member code</th><th>Status</th><th>Membership</th></tr></thead><tbody>{cards.map(c=><tr key={c.card_public_id}><td>{c.card_public_id}</td><td>{c.member_code}</td><td><span className="status">{c.status}</span></td><td>{c.membership_id||'—'}</td></tr>)}</tbody></table>}</>}
